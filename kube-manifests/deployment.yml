apiVersion: apps/v1
kind: Deployment
metadata:
  name: "socio-sync-app"
  labels:
    app: "socio-sync-app"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: "socio-sync-app"
  template:
    metadata:
      labels:
        app: "socio-sync-app"
    spec:
      containers:
      - name: socio-sync-container
        image: kingmaven/socio-sync:latest 
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
    



